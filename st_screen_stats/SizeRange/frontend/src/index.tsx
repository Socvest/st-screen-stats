import React from "react"
import ReactDOM from "react-dom"
import WindowScreenRange from "./WindowScreenRange"

ReactDOM.render(
  <React.StrictMode>
    <WindowScreenRange />
  </React.StrictMode>,
  document.getElementById("root")
)
